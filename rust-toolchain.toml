# The toolchain the image builds with (Dockerfile: rust:1.98-slim-bookworm) and
# Cargo.toml's rust-version. Pinning it here makes a contributor's rustfmt and
# clippy agree with CI's, so formatting and lint results do not drift by host.
[toolchain]
channel = "1.98"
components = ["rustfmt", "clippy"]
