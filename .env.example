DATABASE_URL=postgres://judgebot:judgebot@localhost:5433/judgebot
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
DISCORD_TOKEN=
# Discord (crates/bot). Members holding a role named JUDGE_ROLE rate as judges
# (their score overrides crowd votes). Set GUILD_ID to register /judge in one
# guild instantly instead of globally (global registration can take an hour).
JUDGE_ROLE=Judge
GUILD_ID=
# Most /judge runs in flight at once; further ones get a "busy" reply.
JUDGE_CONCURRENCY=2
# Model spend cap per process in USD (default 5), across every provider. The
# cap reserves the worst case before each call, so values under ~0.45 refuse
# synthesis outright.
JUDGE_MAX_USD=5
# Optional: a judge.toml choosing providers and models per stage (Anthropic
# direct, via a proxy, or on AWS/Bedrock/Vertex with the platform's own
# credentials; any OpenAI-compatible server; a local Ollama). Unset
# reads ./judge.toml if present, else the Anthropic setup above. In Docker
# this is a path inside the container: mount the file (see docker-compose.yml).
JUDGE_CONFIG=
# HTTP API (crates/api) + web client. The api container serves the built
# SolidJS page from WEB_DIST and answers anonymously: per-IP rate limiting
# (API_RATE_LIMIT questions per API_RATE_WINDOW_SECS) protects API spend.
# API_CLIENT_IP picks what the rate limiter buckets on: "peer" (default, the
# socket address) or "cloudflare" (CF-Connecting-IP, which Cloudflare always
# overwrites and callers cannot forge). Set it to cloudflare only when a
# Cloudflare tunnel is the sole route to the API. X-Forwarded-For is never
# trusted: Cloudflare *appends* to a caller-supplied header rather than
# replacing it, so its first hop is attacker-chosen.
# API_ADDR must stay 0.0.0.0 in Docker so cloudflared can reach it across the
# compose network; the published port is what restricts access, not this.
API_ADDR=0.0.0.0:8787
WEB_DIST=web/dist
API_RATE_LIMIT=4
API_RATE_WINDOW_SECS=300
API_CLIENT_IP=peer
# MCP over HTTP (crates/agent mounted by judge-api at /mcp). Unset = no MCP
# endpoint. Set a long random token (`openssl rand -base64 32`, at least 24
# characters) and give it to your MCP client as `Authorization: Bearer ...`.
# The tools include `judge` (spends ANTHROPIC budget under JUDGE_MAX_USD and
# JUDGE_CONCURRENCY, shared with the web page) and agent-driven sessions (no
# model calls). MCP_ALLOWED_HOSTS lists the Host header values the MCP
# transport accepts (comma-separated; the list replaces the default, so name
# the public hostname behind the tunnel AND localhost if you curl on the host,
# e.g. mtgjudge.example.com,localhost); empty keeps the loopback-only default.
# MCP_JUDGE_LIMIT `judge` runs per MCP_JUDGE_WINDOW_SECS through /mcp: the
# blast radius of a leaked token. The web page's per-IP limit does not apply.
MCP_TOKEN=
MCP_ALLOWED_HOSTS=
MCP_JUDGE_LIMIT=20
MCP_JUDGE_WINDOW_SECS=3600

# --- Deployment: Cloudflare Tunnel (docs/DEPLOYMENT.md) -------------------
# Set COMPOSE_PROFILES=tunnel on the server so `docker compose up -d` also
# starts cloudflared. Leave it empty for local development.
COMPOSE_PROFILES=
# Which published image bot/api run. Blank means :latest — set a `sha-<short>`
# tag (from the Publish image workflow) to pin or roll back, then
# `docker compose pull && docker compose up -d`.
JUDGE_IMAGE_TAG=
